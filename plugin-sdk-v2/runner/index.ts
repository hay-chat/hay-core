/**
 * Hay Plugin SDK v2 - Runner
 *
 * This module contains the worker process bootstrap and HTTP server
 * for running plugin instances.
 *
 * The runner is responsible for:
 * - Loading plugin code
 * - Executing plugin hooks (onInitialize, onStart, etc.)
 * - Starting HTTP server with /metadata endpoint
 * - Managing plugin lifecycle
 *
 * @module @hay/plugin-sdk-v2/runner
 */

// Runner implementation will be added in Phase 4
export {};
