/**
 * Attio Plugin Entry Point
 *
 * This is the main entry point for the Attio CRM plugin.
 * The plugin connects to Attio CRM via the MCP (Model Context Protocol) server.
 */

export default function() {
  console.log('Attio plugin loaded');
}
