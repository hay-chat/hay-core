{
  "$schema": "../base/plugin-manifest.schema.json",
  "id": "hay-plugin-stripe",
  "name": "Stripe",
  "description": "Connect your Stripe account to manage payments, customers, subscriptions, and more through Stripe's Model Context Protocol server.",
  "version": "1.0.0",
  "author": "Hay",
  "type": [
    "mcp-connector"
  ],
  "entry": "./dist/index.js",
  "enabled": true,
  "category": "integration",
  "icon": "credit-card",
  "marketplace": {
    "featured": true,
    "tags": [
      "payments",
      "stripe",
      "billing",
      "subscriptions",
      "invoices",
      "customers",
      "ecommerce",
      "finance"
    ]
  },
  "permissions": {
    "api": [
      "mcp"
    ]
  },
  "configSchema": {
    "stripeApiKey": {
      "type": "string",
      "description": "Your Stripe API key. For self-hosted: Use this field to connect with your Stripe account. For managed/cloud: Use OAuth for easier setup (no API key needed). We recommend using restricted API keys to limit access to only required resources.",
      "label": "Stripe API Key",
      "placeholder": "sk_test_... or sk_live_...",
      "required": false,
      "encrypted": true,
      "env": "STRIPE_API_KEY"
    }
  },
  "capabilities": [
    "mcp"
  ],
  "ui": {
    "auth": "oauth2",
    "settings": true
  }
}