[
  {
    "name": "retrieve_balance",
    "label": "Retrieve Balance",
    "description": "Retrieve your current Stripe account balance",
    "input_schema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "create_coupon",
    "label": "Create Coupon",
    "description": "Create a new coupon for discounts",
    "input_schema": {
      "type": "object",
      "properties": {
        "percent_off": {
          "type": "number",
          "description": "Percentage discount (1-100)"
        },
        "amount_off": {
          "type": "number",
          "description": "Fixed amount discount in cents"
        },
        "currency": {
          "type": "string",
          "description": "Three-letter ISO currency code (required if amount_off is specified)"
        },
        "duration": {
          "type": "string",
          "enum": [
            "forever",
            "once",
            "repeating"
          ],
          "description": "How long the coupon lasts"
        },
        "duration_in_months": {
          "type": "number",
          "description": "Number of months the coupon applies (required if duration is 'repeating')"
        },
        "name": {
          "type": "string",
          "description": "Name of the coupon"
        }
      }
    }
  },
  {
    "name": "list_coupons",
    "label": "List Coupons",
    "description": "List all coupons",
    "input_schema": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      }
    }
  },
  {
    "name": "create_customer",
    "label": "Create Customer",
    "description": "Create a new customer",
    "input_schema": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "Customer's email address"
        },
        "name": {
          "type": "string",
          "description": "Customer's name"
        },
        "description": {
          "type": "string",
          "description": "Customer description"
        },
        "phone": {
          "type": "string",
          "description": "Customer's phone number"
        },
        "metadata": {
          "type": "object",
          "description": "Custom metadata for the customer"
        }
      }
    }
  },
  {
    "name": "list_customers",
    "label": "List Customers",
    "description": "List all customers",
    "input_schema": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "Filter by email address"
        },
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      }
    }
  },
  {
    "name": "list_disputes",
    "label": "List Disputes",
    "description": "List all payment disputes",
    "input_schema": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        },
        "status": {
          "type": "string",
          "enum": [
            "warning_needs_response",
            "warning_under_review",
            "warning_closed",
            "needs_response",
            "under_review",
            "charge_refunded",
            "won",
            "lost"
          ],
          "description": "Filter disputes by status"
        }
      }
    }
  },
  {
    "name": "update_dispute",
    "label": "Update Dispute",
    "description": "Update a payment dispute",
    "input_schema": {
      "type": "object",
      "properties": {
        "dispute_id": {
          "type": "string",
          "description": "The ID of the dispute to update"
        },
        "evidence": {
          "type": "object",
          "description": "Evidence to support your case"
        },
        "metadata": {
          "type": "object",
          "description": "Custom metadata"
        },
        "submit": {
          "type": "boolean",
          "description": "Whether to submit the evidence"
        }
      },
      "required": [
        "dispute_id"
      ]
    }
  },
  {
    "name": "create_invoice",
    "label": "Create Invoice",
    "description": "Create a new invoice",
    "input_schema": {
      "type": "object",
      "properties": {
        "customer": {
          "type": "string",
          "description": "The ID of the customer who will be billed"
        },
        "description": {
          "type": "string",
          "description": "Invoice description"
        },
        "auto_advance": {
          "type": "boolean",
          "description": "Whether to automatically finalize and attempt payment"
        }
      },
      "required": [
        "customer"
      ]
    }
  },
  {
    "name": "create_invoice_item",
    "label": "Create Invoice Item",
    "description": "Create an invoice item to add to an invoice",
    "input_schema": {
      "type": "object",
      "properties": {
        "customer": {
          "type": "string",
          "description": "The ID of the customer"
        },
        "amount": {
          "type": "number",
          "description": "Amount in cents"
        },
        "currency": {
          "type": "string",
          "description": "Three-letter ISO currency code"
        },
        "description": {
          "type": "string",
          "description": "Description of the item"
        },
        "invoice": {
          "type": "string",
          "description": "The ID of the invoice to add this item to"
        }
      },
      "required": [
        "customer",
        "amount",
        "currency"
      ]
    }
  },
  {
    "name": "finalize_invoice",
    "label": "Finalize Invoice",
    "description": "Finalize an invoice to make it ready for payment",
    "input_schema": {
      "type": "object",
      "properties": {
        "invoice_id": {
          "type": "string",
          "description": "The ID of the invoice to finalize"
        }
      },
      "required": [
        "invoice_id"
      ]
    }
  },
  {
    "name": "list_invoices",
    "label": "List Invoices",
    "description": "List all invoices",
    "input_schema": {
      "type": "object",
      "properties": {
        "customer": {
          "type": "string",
          "description": "Filter by customer ID"
        },
        "status": {
          "type": "string",
          "enum": [
            "draft",
            "open",
            "paid",
            "uncollectible",
            "void"
          ],
          "description": "Filter by invoice status"
        },
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      }
    }
  },
  {
    "name": "create_payment_link",
    "label": "Create Payment Link",
    "description": "Create a payment link for accepting payments",
    "input_schema": {
      "type": "object",
      "properties": {
        "line_items": {
          "type": "array",
          "description": "Array of line items",
          "items": {
            "type": "object",
            "properties": {
              "price": {
                "type": "string",
                "description": "The ID of the price"
              },
              "quantity": {
                "type": "number",
                "description": "Quantity of the item"
              }
            },
            "required": [
              "price",
              "quantity"
            ]
          }
        }
      },
      "required": [
        "line_items"
      ]
    }
  },
  {
    "name": "list_payment_intents",
    "label": "List Payment Intents",
    "description": "List all payment intents",
    "input_schema": {
      "type": "object",
      "properties": {
        "customer": {
          "type": "string",
          "description": "Filter by customer ID"
        },
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      }
    }
  },
  {
    "name": "create_price",
    "label": "Create Price",
    "description": "Create a new price for a product",
    "input_schema": {
      "type": "object",
      "properties": {
        "product": {
          "type": "string",
          "description": "The ID of the product"
        },
        "unit_amount": {
          "type": "number",
          "description": "Amount in cents"
        },
        "currency": {
          "type": "string",
          "description": "Three-letter ISO currency code"
        },
        "recurring": {
          "type": "object",
          "description": "Recurring billing configuration",
          "properties": {
            "interval": {
              "type": "string",
              "enum": [
                "day",
                "week",
                "month",
                "year"
              ],
              "description": "Billing frequency"
            },
            "interval_count": {
              "type": "number",
              "description": "Number of intervals between billings"
            }
          }
        }
      },
      "required": [
        "product",
        "unit_amount",
        "currency"
      ]
    }
  },
  {
    "name": "list_prices",
    "label": "List Prices",
    "description": "List all prices",
    "input_schema": {
      "type": "object",
      "properties": {
        "product": {
          "type": "string",
          "description": "Filter by product ID"
        },
        "active": {
          "type": "boolean",
          "description": "Filter by active status"
        },
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      }
    }
  },
  {
    "name": "create_product",
    "label": "Create Product",
    "description": "Create a new product",
    "input_schema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Product name"
        },
        "description": {
          "type": "string",
          "description": "Product description"
        },
        "active": {
          "type": "boolean",
          "description": "Whether the product is active"
        },
        "metadata": {
          "type": "object",
          "description": "Custom metadata"
        }
      },
      "required": [
        "name"
      ]
    }
  },
  {
    "name": "list_products",
    "label": "List Products",
    "description": "List all products",
    "input_schema": {
      "type": "object",
      "properties": {
        "active": {
          "type": "boolean",
          "description": "Filter by active status"
        },
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      }
    }
  },
  {
    "name": "create_refund",
    "label": "Create Refund",
    "description": "Create a refund for a charge or payment intent",
    "input_schema": {
      "type": "object",
      "properties": {
        "charge": {
          "type": "string",
          "description": "The ID of the charge to refund"
        },
        "payment_intent": {
          "type": "string",
          "description": "The ID of the payment intent to refund"
        },
        "amount": {
          "type": "number",
          "description": "Amount to refund in cents (omit for full refund)"
        },
        "reason": {
          "type": "string",
          "enum": [
            "duplicate",
            "fraudulent",
            "requested_by_customer"
          ],
          "description": "Reason for the refund"
        }
      }
    }
  },
  {
    "name": "cancel_subscription",
    "label": "Cancel Subscription",
    "description": "Cancel a subscription",
    "input_schema": {
      "type": "object",
      "properties": {
        "subscription_id": {
          "type": "string",
          "description": "The ID of the subscription to cancel"
        },
        "prorate": {
          "type": "boolean",
          "description": "Whether to prorate the cancellation"
        },
        "invoice_now": {
          "type": "boolean",
          "description": "Whether to invoice immediately"
        }
      },
      "required": [
        "subscription_id"
      ]
    }
  },
  {
    "name": "list_subscriptions",
    "label": "List Subscriptions",
    "description": "List all subscriptions",
    "input_schema": {
      "type": "object",
      "properties": {
        "customer": {
          "type": "string",
          "description": "Filter by customer ID"
        },
        "status": {
          "type": "string",
          "enum": [
            "active",
            "past_due",
            "unpaid",
            "canceled",
            "incomplete",
            "incomplete_expired",
            "trialing",
            "all"
          ],
          "description": "Filter by subscription status"
        },
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      }
    }
  },
  {
    "name": "update_subscription",
    "label": "Update Subscription",
    "description": "Update a subscription",
    "input_schema": {
      "type": "object",
      "properties": {
        "subscription_id": {
          "type": "string",
          "description": "The ID of the subscription to update"
        },
        "items": {
          "type": "array",
          "description": "New subscription items",
          "items": {
            "type": "object",
            "properties": {
              "price": {
                "type": "string",
                "description": "Price ID"
              },
              "quantity": {
                "type": "number",
                "description": "Quantity"
              }
            }
          }
        },
        "proration_behavior": {
          "type": "string",
          "enum": [
            "create_prorations",
            "none",
            "always_invoice"
          ],
          "description": "How to handle prorations"
        }
      },
      "required": [
        "subscription_id"
      ]
    }
  },
  {
    "name": "search_stripe_resources",
    "label": "Search Stripe Resources",
    "description": "Search across Stripe resources (customers, invoices, subscriptions, etc.)",
    "input_schema": {
      "type": "object",
      "properties": {
        "resource_type": {
          "type": "string",
          "enum": [
            "customers",
            "invoices",
            "payment_intents",
            "subscriptions",
            "charges"
          ],
          "description": "Type of resource to search"
        },
        "query": {
          "type": "string",
          "description": "Search query string"
        },
        "limit": {
          "type": "number",
          "description": "Number of results to return (default 10)"
        }
      },
      "required": [
        "resource_type",
        "query"
      ]
    }
  },
  {
    "name": "fetch_stripe_resources",
    "label": "Fetch Stripe Resource",
    "description": "Fetch a specific Stripe resource by ID",
    "input_schema": {
      "type": "object",
      "properties": {
        "resource_type": {
          "type": "string",
          "description": "Type of resource (e.g., 'customer', 'invoice', 'subscription')"
        },
        "resource_id": {
          "type": "string",
          "description": "The ID of the resource to fetch"
        }
      },
      "required": [
        "resource_type",
        "resource_id"
      ]
    }
  }
]