[
  {
    "name": "get_product_by_sku",
    "label": "Get Product by SKU",
    "description": "Get detailed information about a product by its SKU (Stock Keeping Unit)",
    "input_schema": {
      "type": "object",
      "properties": {
        "sku": {
          "type": "string",
          "description": "The SKU (Stock Keeping Unit) of the product"
        }
      },
      "required": [
        "sku"
      ]
    }
  },
  {
    "name": "get_product_by_id",
    "label": "Get Product by ID",
    "description": "Get detailed information about a product by its numeric ID",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "number",
          "description": "The ID of the product"
        }
      },
      "required": [
        "id"
      ]
    }
  },
  {
    "name": "search_products",
    "label": "Search Products",
    "description": "Search for products using a query term with pagination support",
    "input_schema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query (product name, description, etc.)"
        },
        "page_size": {
          "type": "number",
          "description": "Number of results per page (default: 10)",
          "default": 10,
          "minimum": 1,
          "maximum": 100
        },
        "current_page": {
          "type": "number",
          "description": "Page number (default: 1)",
          "default": 1,
          "minimum": 1
        }
      },
      "required": [
        "query"
      ]
    }
  },
  {
    "name": "advanced_product_search",
    "label": "Advanced Product Search",
    "description": "Search for products with advanced filtering options and custom field criteria",
    "input_schema": {
      "type": "object",
      "properties": {
        "field": {
          "type": "string",
          "description": "Field to search on (e.g., name, sku, price, status)"
        },
        "value": {
          "type": "string",
          "description": "Value to search for"
        },
        "condition_type": {
          "type": "string",
          "description": "Condition type (eq, like, gt, lt, etc.)",
          "default": "eq",
          "enum": [
            "eq",
            "like",
            "gt",
            "lt",
            "gteq",
            "lteq",
            "neq",
            "in",
            "nin"
          ]
        },
        "page_size": {
          "type": "number",
          "description": "Number of results per page (default: 10)",
          "default": 10,
          "minimum": 1,
          "maximum": 100
        },
        "current_page": {
          "type": "number",
          "description": "Page number (default: 1)",
          "default": 1,
          "minimum": 1
        },
        "sort_field": {
          "type": "string",
          "description": "Field to sort by (default: entity_id)",
          "default": "entity_id"
        },
        "sort_direction": {
          "type": "string",
          "description": "Sort direction (ASC or DESC, default: DESC)",
          "default": "DESC",
          "enum": [
            "ASC",
            "DESC"
          ]
        }
      },
      "required": [
        "field",
        "value"
      ]
    }
  },
  {
    "name": "get_product_categories",
    "label": "Get Product Categories",
    "description": "Get all categories for a specific product by SKU",
    "input_schema": {
      "type": "object",
      "properties": {
        "sku": {
          "type": "string",
          "description": "The SKU (Stock Keeping Unit) of the product"
        }
      },
      "required": [
        "sku"
      ]
    }
  },
  {
    "name": "get_related_products",
    "label": "Get Related Products",
    "description": "Get products related to a specific product by SKU",
    "input_schema": {
      "type": "object",
      "properties": {
        "sku": {
          "type": "string",
          "description": "The SKU (Stock Keeping Unit) of the product"
        }
      },
      "required": [
        "sku"
      ]
    }
  },
  {
    "name": "get_product_stock",
    "label": "Get Product Stock",
    "description": "Get stock information for a product by SKU",
    "input_schema": {
      "type": "object",
      "properties": {
        "sku": {
          "type": "string",
          "description": "The SKU (Stock Keeping Unit) of the product"
        }
      },
      "required": [
        "sku"
      ]
    }
  },
  {
    "name": "get_product_attributes",
    "label": "Get Product Attributes",
    "description": "Get all attributes (base and custom) for a product by SKU",
    "input_schema": {
      "type": "object",
      "properties": {
        "sku": {
          "type": "string",
          "description": "The SKU (Stock Keeping Unit) of the product"
        }
      },
      "required": [
        "sku"
      ]
    }
  },
  {
    "name": "update_product_attribute",
    "label": "Update Product Attribute",
    "description": "Update a specific attribute of a product by SKU",
    "input_schema": {
      "type": "object",
      "properties": {
        "sku": {
          "type": "string",
          "description": "The SKU (Stock Keeping Unit) of the product"
        },
        "attribute_code": {
          "type": "string",
          "description": "The code of the attribute to update (e.g., name, price, description, status, etc.)"
        },
        "value": {
          "description": "The new value for the attribute"
        }
      },
      "required": [
        "sku",
        "attribute_code",
        "value"
      ]
    }
  },
  {
    "name": "get_revenue",
    "label": "Get Revenue",
    "description": "Get the total revenue for a given date range with status filtering",
    "input_schema": {
      "type": "object",
      "properties": {
        "date_range": {
          "type": "string",
          "description": "Date range expression (e.g., 'today', 'yesterday', 'last week', 'this month', 'YTD', or a specific date range like '2023-01-01 to 2023-01-31')"
        },
        "status": {
          "type": "string",
          "description": "Filter by order status (e.g., 'processing', 'complete', 'pending')"
        },
        "include_tax": {
          "type": "boolean",
          "description": "Whether to include tax in the revenue calculation (default: true)",
          "default": true
        }
      },
      "required": [
        "date_range"
      ]
    }
  },
  {
    "name": "get_revenue_by_country",
    "label": "Get Revenue by Country",
    "description": "Get revenue filtered by country for a given date range",
    "input_schema": {
      "type": "object",
      "properties": {
        "date_range": {
          "type": "string",
          "description": "Date range expression (e.g., 'today', 'yesterday', 'last week', 'this month', 'YTD', or a specific date range like '2023-01-01 to 2023-01-31')"
        },
        "country": {
          "type": "string",
          "description": "Country code (e.g., 'US', 'NL', 'GB') or country name (e.g., 'United States', 'The Netherlands', 'United Kingdom')"
        },
        "status": {
          "type": "string",
          "description": "Filter by order status (e.g., 'processing', 'complete', 'pending')"
        },
        "include_tax": {
          "type": "boolean",
          "description": "Whether to include tax in the revenue calculation (default: true)",
          "default": true
        }
      },
      "required": [
        "date_range",
        "country"
      ]
    }
  },
  {
    "name": "get_order_count",
    "label": "Get Order Count",
    "description": "Get the number of orders for a given date range with status filtering",
    "input_schema": {
      "type": "object",
      "properties": {
        "date_range": {
          "type": "string",
          "description": "Date range expression (e.g., 'today', 'yesterday', 'last week', 'this month', 'YTD', or a specific date range like '2023-01-01 to 2023-01-31')"
        },
        "status": {
          "type": "string",
          "description": "Filter by order status (e.g., 'processing', 'complete', 'pending')"
        }
      },
      "required": [
        "date_range"
      ]
    }
  },
  {
    "name": "get_product_sales",
    "label": "Get Product Sales",
    "description": "Get statistics about the quantity of products sold in a given date range with country and status filtering",
    "input_schema": {
      "type": "object",
      "properties": {
        "date_range": {
          "type": "string",
          "description": "Date range expression (e.g., 'today', 'yesterday', 'last week', 'this month', 'YTD', or a specific date range like '2023-01-01 to 2023-01-31')"
        },
        "status": {
          "type": "string",
          "description": "Filter by order status (e.g., 'processing', 'complete', 'pending')"
        },
        "country": {
          "type": "string",
          "description": "Filter by country code (e.g., 'US', 'NL', 'GB') or country name (e.g., 'United States', 'The Netherlands', 'United Kingdom')"
        }
      },
      "required": [
        "date_range"
      ]
    }
  },
  {
    "name": "get_customer_ordered_products_by_email",
    "label": "Get Customer Ordered Products by Email",
    "description": "Get all ordered products for a customer by email address with detailed product information",
    "input_schema": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the customer"
        }
      },
      "required": [
        "email"
      ]
    }
  }
]