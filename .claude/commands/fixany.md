Look for any instances of `any` in the codebase and replace them with the appropriate type.

If you are not sure about the type, ask the user for clarification.

Ideally the types should be in the `entities` folder and that can be used both in the server and the dashboard.
